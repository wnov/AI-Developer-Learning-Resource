# 学习计划 v0

## 目标

系统打好 RL 基础。主干完成后，根据 `learner.md` 中的兴趣信号，在 B1–B4 中选择一个方向展开。

## 节奏

每周约 7 小时：

- 理论会话 ×2，每次约 90 分钟
- 实验块 ×1，约 3 小时
- 每个阶段结束后加一次窗口会话，约 30 分钟

## 当前状态

- 阶段：1 表格方法基础，第 1 周
- 已完成会话：1 次（2026-09-25 诊断）
- 下一次会话：理论 W1-T1，节点 S1.1 + S1.2

诊断结论：阶段 1–2 的形式化内容基本为 L0；S2.4/2.5、S2.7、S3.3、S4.1/4.2、S5.2 为 L1，但多数是经验性判断，机制尚未掌握（见 `learner.md` 的"反复出现的难点"）。前置节点没有达标，所以不压缩后面的阶段，按原节奏从 S1.1 开始。

## 逐周计划（阶段 1–2）

| 周 | 会话 | 节点 / 内容 | 重点 |
| --- | --- | --- | --- |
| 1 | 理论 T1 | S1.1 + S1.2 | 回报与折扣；V 和 Q 的一步展开：r 和 γ 从哪里进入（诊断 Q2 的错误） |
| 1 | 理论 T2 | S1.3 ★ | Bellman 期望方程与最优方程；为什么 V^π 是线性方程组，而 V* 不是 |
| 1 | 实验 | `labs/1-policy-evaluation/` | 网格世界上用两种方法求 V^π：迭代策略评估和直接矩阵求解，对比结果（S1.3 的 L3 证据） |
| 2 | 理论 T1 | S1.4 | 策略评估 + 贪心改进 = 策略迭代；为什么单调改进 |
| 2 | 理论 T2 | S1.5 | 价值迭代；压缩映射与收敛 |
| 2 | 实验 | `labs/1-pi-vi/` | 策略迭代 vs 价值迭代，对比收敛速度（原计划的阶段 1 实验） |
| 2 | 窗口 | MuZero（B2） | |
| 3 | 理论 T1 | S2.1 + S2.2 ★ | MC 与 TD(0)；自举（诊断 Q6 的疑问）；可以用伪标签自训练做类比 |
| 3 | 理论 T2 | S2.3 | n-step 与 TD(λ)：用偏差换方差（诊断 Q8） |
| 3 | 实验 | `labs/2-td-mc/` | 随机游走上的 TD(0) vs MC（SB 例 6.2），S2.2 的 L3 证据 |
| 4 | 理论 T1 | S2.4 + S2.5 | SARSA vs Q-learning；复查诊断 Q5 的机制 |
| 4 | 理论 T2 | S2.6 + S2.7 | ε-greedy；重要性采样 |
| 4 | 实验 | `labs/2-cliff/` | CliffWalking 上的 SARSA vs Q-learning（原计划的阶段 2 实验） |
| 4 | 窗口 | CQL（B1） | |

第 5 周以后沿用下方的阶段时间表，阶段 2 结束时再细化。

## 阶段时间表（估计，诊断后调整）

| 阶段 | 周次 | 实验块 | 阶段末窗口会话 |
| --- | --- | --- | --- |
| 1 表格方法基础 | 第 1–2 周 | 网格世界上实现策略迭代和价值迭代，对比收敛速度 | MuZero：规划与学到的模型（B2） |
| 2 无模型预测与控制 | 第 3–4 周 | CliffWalking 上对比 SARSA 和 Q-learning 的路径差异 | CQL：offline 设定下如何压低 Q 值高估（B1） |
| 3 函数逼近 | 第 5–6 周 | CartPole 上实现 DQN，逐个去掉回放和目标网络观察崩溃 | RND：用预测误差做内在奖励（B4） |
| 4 策略梯度 | 第 7–9 周 | CartPole 上从 REINFORCE 到加 baseline 再到 actor-critic，对比梯度方差 | DreamerV3：在想象中做 actor-critic（B2） |
| 5 现代算法 | 第 10–11 周 | 实现 PPO，消融 clip；在 Pendulum 上跑 SAC | InstructGPT：RLHF 中的 PPO（B3） |

所有实验都使用 Gymnasium 的经典环境，CPU 即可运行。

## 调整记录

每次重排计划都在这里写一行：日期、改了什么、为什么。

- 2026-09-25：诊断后写出第 1–4 周的逐周计划。第 1 周增加"策略评估"实验，因为 S1.3 是 ★ 节点，需要 L3 证据，而原计划阶段 1 只有第 2 周一次实验。阶段 3–5 的时间不变，因为它们的前置节点都是 L0。

## 协议待改

学习者对 `CLAUDE.md` 协议本身的反馈，留到复盘时统一处理。

- 2026-09-25：学习者用手机参加会话，原话"手机打字不方便"。诊断中途已改用选择题。待讨论：理论会话用手机时，推导题怎么作答（手写拍照？只给关键一步？），以及选择题是否一律追问理由（见 `learner.md` 的"只会公式，不懂机制"）。

## 复盘时间

- 第一次：第 3 次会话结束后
- 之后：每 4 周一次
